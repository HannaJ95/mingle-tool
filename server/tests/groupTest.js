import { createGroup, getGroupById, updateGroupStatus } from '../src/db/models/group.js';

// const { data, error } = await createGroup(79);
// const { data, error } = await getGroupById(1);
// const { data, error } = await updateGroupStatus(1, "active");

console.log('data:', data);
console.log('error:', error);
