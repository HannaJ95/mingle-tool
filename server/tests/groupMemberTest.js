import { insertGroupMember, getMembersByGroupId, setMemberReady } from '../src/db/models/groupMember.js';

// const { data, error } = await insertGroupMember(1, 95);
// const { data, error } = await insertGroupMember(1, 97);
// const { data, error } = await getMembersByGroupId(1);
// const { data, error } = await setMemberReady(1, 95);

console.log('data:', data);
console.log('error:', error);
